body { font-family: -apple-system, sans-serif; background: #fff; color: #111; text-align:center; }
.container { padding: 20px; }
.car-image { width: 250px; margin: 10px 0; }
input { width: 80px; }
button { margin: 10px; padding: 8px 15px; font-size:16px; }
.signature { margin-top:20px; font-size:12px; color:#888; }
ul { list-style: none; padding:0; }
li { margin: 10px 0; }
